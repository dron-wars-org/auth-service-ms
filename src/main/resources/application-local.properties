# CORS Configuration
cors.allowed-origins=http://localhost:5173
cors.allowed-methods=GET,POST,PUT,DELETE,PATCH,OPTIONS
cors.allowed-headers=*
cors.allow-credentials=true
cors.max-age=3600

# Database Configuration
spring.datasource.url=jdbc:postgresql://192.168.49.2:30543/dron_wars
spring.datasource.username=admin
spring.datasource.password=banco123
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Flyway Configuration
spring.flyway.enabled=false

# JWT Configuration
jwt.secret=dronwars-super-secret-key-change-in-production-min-256-bits-required-for-hs256-algorithm
jwt.access-token-expiration=900000
jwt.refresh-token-expiration=604800000

# Redis Configuration
spring.data.redis.host=192.168.49.2
spring.data.redis.port=30379
spring.data.redis.timeout=60000

# Kafka Configuration
spring.kafka.bootstrap-servers=192.168.49.2:30092
spring.kafka.producer.key-serializer=org.apache.kafka.common.serialization.StringSerializer
spring.kafka.producer.value-serializer=org.springframework.kafka.support.serializer.JacksonJsonSerializer
kafka.topic.user-events=user-events

# Server Configuration
server.port=8080
